- @title = "ChiliProject Plugins"
- @h1 = (@controller.action_name == 'index') ? "Plugins" : "All Plugins"
- @selected = :plugins

- content_for :actions do
  = link_to( image_tag("/images/plus.png", :class=>"icon") +" Add Plugin", new_plugin_path, :class => "button")

.view_order
  View
  = view_orders(@order)
  
.records_count= record_count_for @plugins, Plugin

= pagination_for @plugins

%table.index#plugins{:style => "clear: both"}
  %tbody
    = render :partial => "plugin", :collection => @plugins

= pagination_for @plugins